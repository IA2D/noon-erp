# Optional: override the local SQLite data directory.
# Keep this on a local disk; network paths are rejected by the recovery policy.
FULLERP_DATA_DIR=

# Development only: open Electron DevTools on startup.
ELECTRON_OPEN_DEVTOOLS=0

# Development only: disable Vite HMR file watching.
DISABLE_HMR=false
